<h1>Edit Page</h1>

<%= form_for(@object) do |f| %>
                            
<!-- the argument :f below is to pass the f into the partial -->
<%= render 'fields', :f => f%>

<div class="actions">
  <%= f.submit "Update"%>
</div>

<% end %> 
