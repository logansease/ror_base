<% followed_id = "" if local_assigns[:followed_id].nil? %>
<% type = "" if local_assigns[:type].nil? %>


<%= form_for(current_user.user_relationships.find_by_followed_id(followed_id),
				:html => {:method => :delete, :id => "connect_button", :class => "new_button icon-ok-1 right" },
			    :remote =>true) do |f| %>


 	<%= f.submit "connected!" %>


<% end%>